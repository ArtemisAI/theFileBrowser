# ---------------------------------------------------------------------------
#  Environment template for the Artemis File-Browser stack
# ---------------------------------------------------------------------------
#  Copy this file to `.env` and adjust values as required for local
#  development.  **Never commit your real credentials** – keep them in the
#  un-tracked `.env` file.
# ---------------------------------------------------------------------------

# UID & GID that the File Browser container should run as (defaults: 1000)
PUID=1000
PGID=1000

# Base URL of the File Browser API – used by the React front-end at runtime.
# When the UI runs inside docker-compose, this should be
#   VITE_API=http://filebrowser
# When running Vite locally (npm run dev) outside Compose, point to
#   VITE_API=http://localhost:8080
VITE_API=http://localhost:8080

# Example of external services used across the wider Artemis project.
#OLLAMA_HOST=
#N8N_HOST=
#KOKORO_HOST=
